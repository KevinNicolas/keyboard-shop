import { motion } from 'framer-motion';

import AuthStyles from './Auth.styles';

export const Auth = () => {
  return (
    <AuthStyles>
      <span>Login</span>
    </AuthStyles>
  );
};

export default Auth;
